# syntax=docker/dockerfile:1.7

# ============================================================
# Unified C++ Toolchain / Dev / Runtime Images
#
# Targets:
#   runtime-base
#   runtime-debug-base
#   builder-gcc
#   builder-clang
#   dev-gcc
#   dev-clang
#
# Suggested image tags:
#   cpp-runtime-base:bookworm-v1
#   cpp-runtime-debug-base:bookworm-v1
#   cpp-builder-gcc:bookworm-v1
#   cpp-builder-clang:bookworm-v1
#   cpp-dev-gcc:bookworm-v1
#   cpp-dev-clang:bookworm-v1
# ============================================================

ARG DEBIAN_VERSION=bookworm
ARG DEBIAN_FRONTEND=noninteractive
ARG RUNTIME_BASE_IMAGE=gcr.io/distroless/cc-debian12:nonroot
ARG UV_VERSION=0.11.10
ARG CONAN_VERSION=2.28.1
ARG PYTHON_VERSION=3.12.0
ARG USERNAME=dev
ARG UID=1000
ARG GID=1000
ARG IMAGE_SOURCE=unknown
ARG IMAGE_REVISION=unknown
ARG IMAGE_VERSION=dev

FROM ghcr.io/astral-sh/uv:${UV_VERSION} AS uv-bin
# ============================================================
# 1. os-base
#    所有镜像共享的最小基础层
# ============================================================
FROM debian:${DEBIAN_VERSION}-slim AS os-base

ARG DEBIAN_FRONTEND
ARG IMAGE_SOURCE
ARG IMAGE_REVISION
ARG IMAGE_VERSION

ENV LANG=C.UTF-8 \
    LC_ALL=C.UTF-8

LABEL org.opencontainers.image.title="C++ toolchain images" \
      org.opencontainers.image.description="Shared C++ runtime, builder, and development images" \
      org.opencontainers.image.source="${IMAGE_SOURCE}" \
      org.opencontainers.image.revision="${IMAGE_REVISION}" \
      org.opencontainers.image.version="${IMAGE_VERSION}" \
      org.opencontainers.image.licenses="NOASSERTION"

RUN apt-get update && apt-get install -y --no-install-recommends \
        ca-certificates \
        tzdata \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old


# ============================================================
# 2. runtime-base
#    生产运行基础镜像
# ============================================================
FROM os-base AS runtime-layout

ARG APP_HOME=/opt/app
ARG DEBIAN_FRONTEND

RUN mkdir -p ${APP_HOME}/bin ${APP_HOME}/lib ${APP_HOME}/etc ${APP_HOME}/log

FROM ${RUNTIME_BASE_IMAGE} AS runtime-base

ARG APP_HOME=/opt/app

ENV APP_HOME=${APP_HOME} \
    PATH="${APP_HOME}/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
    LD_LIBRARY_PATH="${APP_HOME}/lib"

COPY --from=runtime-layout --chown=65532:65532 ${APP_HOME} ${APP_HOME}

WORKDIR ${APP_HOME}

USER 65532:65532


# ============================================================
# 3. runtime-debug-base
#    Debug / 测试运行基础镜像
# ============================================================
FROM os-base AS runtime-debug-base

ARG APP_HOME=/opt/app
ARG DEBIAN_FRONTEND

ENV APP_HOME=${APP_HOME} \
    PATH="${APP_HOME}/bin:${PATH}" \
    LD_LIBRARY_PATH="${APP_HOME}/lib"

RUN groupadd -g 65532 app \
    && useradd \
        --uid 65532 \
        --gid app \
        --home-dir ${APP_HOME} \
        --shell /usr/sbin/nologin \
        app \
    && mkdir -p ${APP_HOME}/bin ${APP_HOME}/lib ${APP_HOME}/etc ${APP_HOME}/log \
    && chown -R app:app ${APP_HOME}

RUN apt-get update && apt-get install -y --no-install-recommends \
        libstdc++6 \
        libgcc-s1 \
        gdb \
        strace \
        procps \
        iproute2 \
        netcat-openbsd \
        curl \
        file \
        less \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old

USER app

WORKDIR ${APP_HOME}

CMD ["/bin/sh"]


# ============================================================
# 4. builder-base
#    C++ 编译基础层
# ============================================================
FROM os-base AS builder-base

ARG DEBIAN_FRONTEND
ARG CONAN_VERSION
ARG PYTHON_VERSION

ENV PIP_DISABLE_PIP_VERSION_CHECK=1 \
    CONAN_HOME=/root/.conan2 \
    CCACHE_DIR=/root/.cache/ccache \
    UV_CACHE_DIR=/opt/uv/cache \
    UV_PYTHON_INSTALL_DIR=/opt/uv/python \
    PATH="/opt/conan/bin:${PATH}"

RUN apt-get update && apt-get install -y --no-install-recommends \
        build-essential \
        gcc \
        g++ \
        cmake \
        ninja-build \
        make \
        pkg-config \
        git \
        ccache \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old

COPY --from=uv-bin /uv /uvx /bin/

RUN mkdir -p /opt/uv/cache /opt/uv/python \
    && uv python install "${PYTHON_VERSION}" \
    && uv venv --python "${PYTHON_VERSION}" /opt/conan \
    && uv pip install --python /opt/conan/bin/python --no-cache "conan==${CONAN_VERSION}" \
    && chmod -R a+rX /opt/uv /opt/conan \
    && echo 'export PATH="/opt/conan/bin:$PATH"' > /etc/profile.d/conan.sh \
    && chmod 0644 /etc/profile.d/conan.sh \
    && ln -sf /opt/conan/bin/conan /usr/local/bin/conan \
    && conan --version \
    && find /opt/uv /opt/conan -type d -name __pycache__ -prune -exec rm -rf {} + \
    && find /opt/uv /opt/conan -type f -name '*.pyc' -delete \
    && rm -rf /opt/uv/cache/*  # 清理 uv 缓存以减小镜像体积

RUN cat > /usr/local/bin/conan-detect-gnu20-profile <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

conan profile detect --force
profile="$(conan profile path default)"

if grep -q '^compiler.cppstd=' "${profile}"; then
    sed -i 's/^compiler.cppstd=.*/compiler.cppstd=gnu20/' "${profile}"
else
    sed -i '/^\[settings\]/a compiler.cppstd=gnu20' "${profile}"
fi

conan profile show -pr default
EOF

RUN chmod 0755 /usr/local/bin/conan-detect-gnu20-profile

WORKDIR /workspace

CMD ["/bin/bash"]


# ============================================================
# 5. builder-gcc
#    CI/CD GCC 编译镜像
# ============================================================
FROM builder-base AS builder-gcc

ENV CC=gcc \
    CXX=g++

RUN conan-detect-gnu20-profile

WORKDIR /workspace

CMD ["/bin/bash"]


# ============================================================
# 6. builder-clang
#    CI/CD Clang 编译镜像
# ============================================================
FROM builder-base AS builder-clang

ARG DEBIAN_FRONTEND

RUN apt-get update && apt-get install -y --no-install-recommends \
        clang \
        clang-format \
        clang-tidy \
        lld \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old

ENV CC=clang \
    CXX=clang++

RUN conan-detect-gnu20-profile

WORKDIR /workspace

CMD ["/bin/bash"]


# ============================================================
# 7. dev-base
#    开发镜像公共基础层
# ============================================================
FROM builder-base AS dev-base

ARG DEBIAN_FRONTEND
ARG USERNAME
ARG UID
ARG GID

RUN apt-get update && apt-get install -y --no-install-recommends \
        valgrind \
        strace \
        procps \
        iproute2 \
        iputils-ping \
        netcat-openbsd \
        curl \
        less \
        vim \
        bash-completion \
        file \
        tree \
        unzip \
        tar \
        xz-utils \
        openssh-server \
        sudo \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old

RUN groupadd -g ${GID} ${USERNAME} \
    && useradd -m -u ${UID} -g ${GID} -s /bin/bash ${USERNAME} \
    && mkdir -p /home/${USERNAME}/.conan2 \
    && mkdir -p /home/${USERNAME}/.cache/ccache \
    && mkdir -p /home/${USERNAME}/.cache/uv \
    && mkdir -p /home/${USERNAME}/.local/bin \
    && mkdir -p /home/${USERNAME}/.local/share/uv/python \
    && mkdir -p /home/${USERNAME}/.ssh \
    && mkdir -p /workspace \
    && mkdir -p /run/sshd \
    && chown -R ${USERNAME}:${USERNAME} /home/${USERNAME} /workspace \
    && chmod 700 /home/${USERNAME}/.ssh \
    && echo "${USERNAME} ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/${USERNAME} \
    && chmod 0440 /etc/sudoers.d/${USERNAME}


RUN cat > /home/${USERNAME}/.dev-shellrc <<'EOF'
# ===== Custom aliases =====
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias cls='clear'
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# ===== ccache helpers =====
alias ccs='ccache -s'
alias ccz='ccache -z'

# ===== CMake helpers =====
alias cb='cmake --build build'
alias ct='ctest --test-dir build --output-on-failure'
EOF

RUN cat >> /home/${USERNAME}/.bashrc <<'EOF'

if [ -f "$HOME/.dev-shellrc" ]; then
    . "$HOME/.dev-shellrc"
fi
EOF

RUN cat >> /root/.bashrc <<EOF

if [ -f "/home/${USERNAME}/.dev-shellrc" ]; then
    . "/home/${USERNAME}/.dev-shellrc"
fi
EOF

RUN cat > /home/${USERNAME}/.bash_profile <<'EOF'
if [ -f "$HOME/.bashrc" ]; then
    . "$HOME/.bashrc"
fi
EOF

RUN cat > /home/${USERNAME}/.profile <<'EOF'
if [ "$BASH" ] && [ -f "$HOME/.bashrc" ]; then
    . "$HOME/.bashrc"
fi
EOF

RUN chown ${USERNAME}:${USERNAME} \
        /home/${USERNAME}/.dev-shellrc \
        /home/${USERNAME}/.bashrc \
        /home/${USERNAME}/.bash_profile \
        /home/${USERNAME}/.profile

RUN mkdir -p /etc/ssh/sshd_config.d \
    && { \
        echo 'PasswordAuthentication no'; \
        echo 'PubkeyAuthentication yes'; \
        echo 'PermitRootLogin no'; \
    } > /etc/ssh/sshd_config.d/20-dev-container.conf

ENV DEV_USERNAME=${USERNAME} \
    CONAN_HOME=/home/${USERNAME}/.conan2 \
    CCACHE_DIR=/home/${USERNAME}/.cache/ccache \
    UV_CACHE_DIR=/home/${USERNAME}/.cache/uv \
    UV_PYTHON_INSTALL_DIR=/home/${USERNAME}/.local/share/uv/python \
    PATH="/home/${USERNAME}/.local/bin:${PATH}"

RUN cat > /usr/local/bin/dev-container-entrypoint <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
    set -- /usr/sbin/sshd -D -e
fi

if [[ "${1}" == "sshd" || "${1}" == "/usr/sbin/sshd" ]]; then
    exec /usr/sbin/sshd "${@:2}"
fi

if [[ "$(id -u)" == "0" ]]; then
    exec sudo -E -H -u "${DEV_USERNAME:-dev}" -- "$@"
fi

exec "$@"
EOF

RUN chmod 0755 /usr/local/bin/dev-container-entrypoint

WORKDIR /workspace

EXPOSE 22

ENTRYPOINT ["/usr/local/bin/dev-container-entrypoint"]

CMD ["/usr/sbin/sshd", "-D", "-e"]


# ============================================================
# 8. dev-gcc
#    GCC 开发镜像
# ============================================================
FROM dev-base AS dev-gcc

ARG DEBIAN_FRONTEND
ARG USERNAME

USER root

RUN apt-get update && apt-get install -y --no-install-recommends \
        gdb \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old

USER ${USERNAME}

ENV CC=/usr/bin/gcc \
    CXX=/usr/bin/g++

# 在设置了环境变量后，生成普通用户的 GCC Conan Profile
RUN conan-detect-gnu20-profile \
    && echo 'export CC=/usr/bin/gcc' >> /home/${USERNAME}/.bashrc \
    && echo 'export CXX=/usr/bin/g++' >> /home/${USERNAME}/.bashrc

USER root
RUN printf '%s\n' \
        "SetEnv CC=/usr/bin/gcc CXX=/usr/bin/g++ CONAN_HOME=/home/${USERNAME}/.conan2 CCACHE_DIR=/home/${USERNAME}/.cache/ccache UV_CACHE_DIR=/home/${USERNAME}/.cache/uv UV_PYTHON_INSTALL_DIR=/home/${USERNAME}/.local/share/uv/python" \
        > /etc/ssh/sshd_config.d/30-toolchain-env.conf

WORKDIR /workspace
CMD ["/usr/sbin/sshd", "-D", "-e"]


# ============================================================
# 9. dev-clang
#    Clang / LLVM 开发镜像
# ============================================================
FROM dev-base AS dev-clang

ARG DEBIAN_FRONTEND
ARG USERNAME

USER root

RUN apt-get update && apt-get install -y --no-install-recommends \
        clang \
        clangd \
        clang-format \
        clang-tidy \
        lld \
        lldb \
        gdb \
    && rm -rf \
        /var/lib/apt/lists/* \
        /usr/share/doc/* \
        /usr/share/man/* \
        /usr/share/info/* \
        /usr/share/lintian/* \
        /var/cache/debconf/*-old

USER ${USERNAME}

ENV CC=/usr/bin/clang \
    CXX=/usr/bin/clang++

# 关键修复：此时环境变量 CC=clang 已经生效，Conan 会正确识别出 Clang 并生成对应的 Profile
RUN conan-detect-gnu20-profile \
    && echo 'export CC=/usr/bin/clang' >> /home/${USERNAME}/.bashrc \
    && echo 'export CXX=/usr/bin/clang++' >> /home/${USERNAME}/.bashrc

USER root
RUN printf '%s\n' \
        "SetEnv CC=/usr/bin/clang CXX=/usr/bin/clang++ CONAN_HOME=/home/${USERNAME}/.conan2 CCACHE_DIR=/home/${USERNAME}/.cache/ccache UV_CACHE_DIR=/home/${USERNAME}/.cache/uv UV_PYTHON_INSTALL_DIR=/home/${USERNAME}/.local/share/uv/python" \
        > /etc/ssh/sshd_config.d/30-toolchain-env.conf

WORKDIR /workspace
CMD ["/usr/sbin/sshd", "-D", "-e"]
